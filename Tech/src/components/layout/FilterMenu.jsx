import FilterButton from "../items/Buttons/FIlterButton"
import Select from "../input/Select"

import { useState, useEffect, useRef } from "react";

function FilterMenu() {

        return(
        <>
            {/* <FilterButton HandleOnClick={aparecerMenu}></FilterButton>

            <Select closeMenu={closeMenu}
            ref={menuRef}
            customclass={`transform transition-all duration-1000 ${
                aparecerMenu
                    ? isAnimating
                        ? 'translate-y-0 h-full sm:h-[500px]'
                        : 'translate-y-0 h-[500px]'
                    : isAnimating
                    ? 'h-10 border-none -translate-y-1'
                    : '-translate-y-1 h-full border-none'
            }`}
            ></Select> */}
        </>
    )
};

export default FilterMenu